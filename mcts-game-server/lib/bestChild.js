'use strict';
{
	module.exports = function() {
		var knowledgeBase = require('./knowledgeBase');
			
		// a call to neo4j to find the best child of the provided state
		
		// return the action edge connecting that parent and child
	}
}