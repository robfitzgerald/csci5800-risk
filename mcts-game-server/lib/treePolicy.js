'use strict';
{
	module.exports = function() {
		var knowledgeBase = require('knowledgeBase');

		// construct the query/ies to call neo4j and select the next node
		// via the multi armed bandit selection process
	}
}